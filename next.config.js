/** @type {import('next').NextConfig} */
const nextConfig = {
  // Make sure API routes are enabled (they should be by default)
  experimental: {
    // Any experimental features you need
  },
  // Add any other necessary configuration
}

module.exports = nextConfig